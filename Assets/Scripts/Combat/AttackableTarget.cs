using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace WarRoad.Combat
{
    [RequireComponent(typeof(Health))] // this will automatically add the health classi if it is missing on the object
    public class AttackableTarget : MonoBehaviour
    {

    }
}
